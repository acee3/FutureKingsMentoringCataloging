# AGENTS

## Type Organization

- Put all project-local types, dataclasses, and `TypedDict` definitions in `app_types.py`.
- Import shared app types from `app_types.py` instead of redefining them in feature modules.
